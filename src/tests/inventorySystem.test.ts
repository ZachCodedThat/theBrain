import react from "react";
import { render, screen } from "@testing-library/react";
import "@testing-library/jest-dom/extend-expect";
import { InventorySystem } from "../components/InventorySystem";

// we can mock data to test the systems functionality and ability to handle edge cases.

// test the systems ability to determine low inventory and activate the out of stock workflow.

// Tests the system to ensure the dispenseFLag is set properly when a valid transaction is made.
